name: Main

on: [push]

jobs:
  actiontrigger:
    runs-on: ubuntu-latest
    name: Action Trigger
    steps:
    - name: Action Trigger Step
      id: action-trigger-step
      uses: ./.github/actions/test-action
    - name: Use output
      run: echo "The output was ${{ steps.action-trigger-step.some-secretvalue }}"